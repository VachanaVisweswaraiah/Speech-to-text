name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Cache uv packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Create virtual environment
        run: |
          uv venv

      - name: Install dependencies with uv
        run: |
          uv pip install -r requirements.txt

      - name: Check code formatting with Black
        run: |
          uv run black --check --verbose .

      - name: Lint with Pylint
        run: |
          uv run pylint config.py audio_processor.py transcriber.py auth.py storage.py batch_processor.py monitoring.py --exit-zero
        continue-on-error: true

      - name: Lint with Flake8
        run: |
          uv run flake8 app.py config.py audio_processor.py transcriber.py auth.py storage.py batch_processor.py monitoring.py test_audio_processor.py test_transcriber.py --max-line-length=100 --extend-ignore=E203,W503
        continue-on-error: true

      - name: Run unit tests with pytest
        run: |
          uv run pytest test_*.py -v --cov=. --cov-report=xml --cov-report=html --cov-report=term

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: Upload coverage artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report-${{ matrix.python-version }}
          path: htmlcov/

  security-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Create virtual environment
        run: |
          uv venv

      - name: Install dependencies
        run: |
          uv pip install bandit

      - name: Run security checks with Bandit
        run: |
          uv run bandit -r . -x .venv,__pycache__,.pytest_cache -ll --skip B101 --exit-zero
        continue-on-error: true

  code-quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Create virtual environment
        run: |
          uv venv

      - name: Install dependencies
        run: |
          uv pip install -r requirements.txt

      - name: Check code format without fixing
        run: |
          uv run black --check . --diff
